import React from 'react';

export interface TitleIconProps {
  title: string,
  icon: SVGAElement,
}

export const TeamHeader: React.FC<TitleIconProps> = (props) => {
  return (
    <div>
      TitleIconProps
    </div>
  );
};
